import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Take Your Tram',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
